import { GoogleBookVolume } from '../../types/models/googleBook';

// GoogleBookStateの型定義はstore/types.tsで既に定義されているため、
// ここではインターフェースを定義せず、既存の型を参照するようにします
import { GoogleBookState } from '../types';

// 必要に応じて追加のタイプを定義
